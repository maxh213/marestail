## Gate

Run `marestail gate` before finishing any change and keep going until it prints GATE PASSED. It enforces 100% coverage, CRAP at most 4, no comments or docstrings, the dependency direction, and a clean Sonar. Fix the code, never the gate.

Documentation lives where this repo already keeps it; update those places and add no new documentation files.
